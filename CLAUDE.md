# Глобальные правила Claude Code

## Язык
- Язык общения: **русский** — всегда

## Жёсткие правила
- **ПЕРВОЕ СООБЩЕНИЕ**: при получении первого сообщения в любой сессии — по умолчанию **ОБЯЗАТЕЛЬНО** запустить `/brainstorming` ДО любых других действий. Сначала мозговой штурм → выбор направления → потом работа. **Единственное исключение**: если первое сообщение — явно системная/межсервисная архитектура (подходит под Fable/`@architect`), НЕ запускать brainstorming молча, а сначала спросить выбор: «`/brainstorming` (разобрать подходы) или `@architect` (сразу глубокое проектирование на Fable)?» — и дождаться ответа. Для всех прочих задач brainstorming первым, без исключений.
- **Чтение файлов — ВСЕГДА без спроса**. Читай, ищи, анализируй свободно. Разрешение нужно только на РЕДАКТИРОВАНИЕ.
- **НЕ редактировать файлы без явного одобрения** — сначала показать чек-лист изменений, дождаться "да"
- **СНАЧАЛА УТОЧНЯЮЩИЕ ВОПРОСЫ**: перед написанием ТЗ, спек, документации или существенного кода — **сначала** задать 3–5 уточняющих вопросов (скоуп, модель данных, пограничные случаи). **Дождаться ответов**. Не начинать драфт до подтверждения скоупа. Исключение: тривиальные правки (опечатка, однострочник).
- **КОНТЕКСТ ПРОЕКТА ДО ИССЛЕДОВАНИЯ**: перед чтением кода уточнить **какой проект** и **какой стек**, если это не очевидно из запроса. Стек и специфику определять из проектного `CLAUDE.md` и манифестов (`go.mod` / `package.json` / `composer.json` / `pubspec.yaml`), а не из допущений — у разных проектов разные стек, структура и конвенции. Не догадываться — спрашивать при неоднозначности.
- **Автоподбор скиллов**: при любой задаче — определить подходящий скилл, сказать пользователю "Запускаем /[скилл] или пропускаем?", дождаться ответа
- **НЕ редактировать файлы с credentials** (.env, config с паролями, keys.json, credentials.json)
- **АВТОМАТИЧЕСКИЙ /review ПОСЛЕ КОДА**: после написания/изменения любого кода (одного файла или серии) — **обязательно** предложить запустить `/review` перед тем, как сказать "готово" / отдать пользователю / передать другому. Формат: "Код собран → запускаем /review или пропускаем?". Без апрува пользователя ревью не запускать, но **предлагать всегда**, не ждать пока спросят.

## Глобальные скиллы

### Основные (свои)
| Скилл | Когда использовать |
|-------|-------------------|
| `/brainstorming` | **ОБЯЗАТЕЛЬНО первое сообщение** — мозговой штурм, подходы, выбор направления |
| `/build` | **Оркестратор** — полный цикл: БА → Архитектура → Docs → TDD → Код → Review → DevOps |
| `/research` | Исследование модуля, архитектуры, зависимостей |
| `/deep-research` | Глубокий веб-ресёрч: технологии, сравнения, рынок (4 режима глубины) |
| `/review` | Ревью перед деплоем — блокеры, побочные эффекты |
| `/docs` | Документация для модуля — API, БД, бизнес-логика, dev-guide |
| `/report` | Итоговый отчёт по модулю |
| `/test` | Написание тестов для модуля/функции (TDD) |
| `/tz` | Создание технического задания — оценка масштаба, уточняющие вопросы, драфт, /review |
| `/audit-server` | Аудит продакшн-сервера — SSH-инвентаризация, чек-лист с апрувом, бэкап перед изменениями |
| `/spec-to-code` | TDD-пайплайн: ТЗ → тесты (RED) → имплементация (GREEN) → /review. Полуавтомат с режимом "беги дальше" |

### Аудит и качество кода
| Скилл | Когда использовать |
|-------|-------------------|
| `/api-contract-guardian` | Проверка API контрактов и схем — валидация запросов/ответов, баги на стыках |
| `/cicd-quick-setup` | Деплой-пайплайн под стек (Docker, GitHub Actions, PM2, VPS) с защитой существующих конфигов |
| `/dependency-optimizer` | Аудит зависимостей — уязвимости, неиспользуемые пакеты, тяжёлые замены |
| `/error-handling-standardizer` | Единая система обработки исключений и логирования по всему проекту |
| `/performance-scanner` | Узкие места, медленные операции, ресурсоёмкие паттерны — анализ без правок |

### Из Superpowers (дисциплина и автоматизация)
| Скилл | Когда использовать |
|-------|-------------------|
| `/systematic-debugging` | 4-фазный дебаг: root cause → pattern → hypothesis → fix |
| `/test-driven-development` | Жёсткий RED-GREEN-REFACTOR — перед написанием любого кода |
| `/verification-before-completion` | Проверка "готово" с доказательствами — перед коммитом/PR |
| `/subagent-driven-development` | Выполнение плана через субагентов с 2-stage review |
| `/dispatching-parallel-agents` | 2+ независимых задачи — параллельные субагенты |
| `/receiving-code-review` | Приём фидбека: verify → evaluate → implement (не слепо) |
| `/finishing-a-development-branch` | Merge/PR/cleanup после завершения работы |
| `/using-git-worktrees` | Изоляция feature-работы через git worktree |

### Обслуживание сетапа (ревизоры — только отчёт, правки по апруву)
| Скилл | Когда использовать |
|-------|-------------------|
| `/skill-stocktake` | Ревизия всех скиллов на качество и дубли — вердикты Keep/Improve/Update/Retire/Merge |
| `/context-budget` | Аудит токен-бюджета — кто из CLAUDE.md/правил/скиллов/MCP сколько съедает контекста |
| `/rules-distill` | Принципы из 2+ скиллов поднять в общие правила (`shared_claude/rules/`) |

## Роутинг моделей по задачам

**Дефолт главного цикла — Opus 5.** Роутинг уводит вниз на Sonnet 5/Haiku 4.5, где скорость сжигания лимита важнее интеллекта, и вверх на Fable 5 — только для системной архитектуры. Вторая ось — **effort** (см. ниже): она влияет на расход квоты сильнее, чем выбор модели.

### Субагенты (~/.claude/agents/)
| Агент | Модель | effort | Для чего |
|-------|--------|--------|----------|
| `architect` | Fable 5 | `xhigh` | системная/межсервисная архитектура, глубокий архитектурный анализ (**ЕДИНСТВЕННАЯ точка Fable**) |
| `researcher` | Sonnet 5 | `medium` | фоновый разбор кодовой базы / веб-ресёрч (read-only) |
| `mechanic` | Haiku 4.5 | `low` | механика: grep, поиск файлов, переименования, форматирование |

### Карта: тип задачи → модель
| Тип задачи | Модель |
|-----------|--------|
| Системная/межсервисная архитектура, глубокий архитектурный анализ | **Fable 5** (`@architect`) |
| Архитектура в рамках модуля | Opus 5 (эскалация на `@architect`/Fable — по требованию, если дизайн реально сложный) |
| Сложное pure-logic проектирование (алгоритм/модель данных, без security) | Opus 5; опц. `@architect` (Fable) |
| Планирование + верификация плана | черновик Sonnet 5/Opus 5 → проверка Fable (`/plan`) |
| Мозговой штурм, ТЗ/спеки, код, вёрстка | Opus 5 |
| Ревью кода, ревью PR лидов | Opus 5 |
| Security-ревью кода, дебаг, рефакторинг, performance, БД (миграции/ревью) | Opus 5 |
| **Offensive-security: pentest, CTF, аудит прод-сервера, ротация учёток, выдача/настройка доступов (SSH-учётки, ключи, права)** | **Opus 4.8** (пин — см. правило про классификаторы) |
| DevOps/CI-CD: конфиги, пайплайны, docker | Sonnet 5 |
| API-контракты: сверка схем запросов/ответов | Sonnet 5 |
| Инцидент на проде (зависон/OOM/watchdog), мета-работа (правка скиллов/CLAUDE.md/промптов) | Opus 5 |
| Research кодовой базы, глубокий веб-ресёрч | Sonnet 5 (`@researcher`) |
| Тесты (TDD), прогон тестов, обработка ошибок (+финал ревью Opus) | Sonnet 5 |
| Документация, отчёты, суммаризация логов, аудит зависимостей, задания вайб-кодерам, ad-hoc SQL, ревизия скиллов/бюджета | Sonnet 5 |
| Механика: grep/поиск/переименования/форматирование, git-хозяйство (коммиты/PR/changelog), i18n | Haiku 4.5 (`@mechanic`) |
| **Всё, чего нет в этой карте** | **Opus 5** |

### Уровни effort (вторая ось)
Глобальный дефолт — `high` (задан в `~/.claude/settings.json`). Не держать глобальный `xhigh`: он применяется и к grep, и к коммитам, и к докам.

| Уровень | Когда |
|---------|-------|
| `xhigh` | кодинг, агентные многошаговые задачи, длинные автономные прогоны → `/effort xhigh` |
| `high` | **дефолт**: диалог, ТЗ, ревью, доки, мета-работа |
| `medium` | объёмная рутина, где можно разменять интеллект на скорость |
| `low` | механика, короткие точечные правки |
| `max` | только когда корректность важнее стоимости; склонен к переобдумыванию — не ставить по умолчанию |

- Разовое углубление без смены сессии — слово `ultrathink` в промпте (остальные «think hard» и т.п. движок не распознаёт).
- У субагентов effort задаётся в frontmatter и перебивает сессионный.

### Правила
- **Тип задачи не покрыт явным правилом → Opus 5** (безопасный дефолт).
- **Классификаторы и авто-фолбэк**: Opus 5 и Fable 5 несут cyber/bio-классификаторы. Claude Code сам перезапускает помеченный запрос: Fable cyber→Opus 4.8, Fable bio→Opus 5, Opus 5 cyber→Opus 4.8; bio на Opus 5 = `refusal` без фолбэка. **Фолбэк переводит на другую модель всю сессию** — на длинных аудитах и pentest-задачах не полагаться на него, а стартовать сессию `/model claude-opus-4-8` заранее.
- **Fable-задача → ВСЕГДА предлагать `@architect`**: на ЛЮБОМ шаге сессии (не только в начале, не только первым сообщением), как только задача подходит под Fable — системная/межсервисная архитектура, схемы данных, стратегия миграции, разбор системы целиком — **обязательно** предложить `@architect`, а НЕ делать её молча на Opus. Формат: «Это Fable-задача → зову `@architect` или пропускаем?».
- **Fable — только `architect`** (системная архитектура + глубокий анализ). Ревью, security, аудит сервера, дебаг, прод — **Opus** (5 или 4.8 по правилу выше).
- **Fable = системная/межсервисная архитектура; Opus 5 = архитектура в рамках одного модуля** (модульную можно эскалировать на `@architect`, если дизайн реально сложный — но по умолчанию Opus 5: дешевле и держит контекст диалога). Сложное pure-logic проектирование (алгоритм/модель данных, без security-контекста) — опционально на `@architect`. Диалоговое проектирование — переключить главный цикл `/model fable` вручную (субагент не задаёт вопросы в диалоге).
- **Фолбэк Fable→Opus по недоступности**: content-refusal ловит харнесс сам (см. выше). Ручной `/fable-off` — клапан по квоте: на Team Premium Fable упирается в 50% недельной квоты. Вернуть — `/fable-on`. Если вызов `architect` упал по недоступности/лимиту — повтори тот же вызов через Agent с `model: opus` и сообщи пользователю.
- **Лимит (advisory)**: если в statusline видно использование ≥85% и >1ч до сброса — предложи `/fable-off`. Надёжного авто-триггера нет, только предложение глазами.

### Делегирование субагентам
Opus 5 делегирует охотнее прошлых моделей. Каждый субагент умножает стоимость и время: заново поднимает контекст, исследует, отчитывается — а потом ты ещё читаешь отчёт.
- **НЕ делегировать** то, что закрывается самому за несколько tool-call (пара чтений файла, простой поиск, мелкая правка).
- **НЕ плодить ad-hoc субагентов для перепроверки собственной работы** — верификация живёт в главном цикле. **Исключение:** ревью/QA-этапы, явно заложенные в конструкцию скилла (`/plan` шаг 3, `/build` этапы 7–9, two-stage review в `subagent-driven-development`) — их пропускать нельзя, это не «лишнее делегирование».
- Одна задача → один субагент. Параллельные — только под реально независимые крупные направления, не под нарезку одной задачи на куски.
- Не больше 20 параллельных агентов без явной просьбы пользователя.
- Делегировал — доверься: не переделывать работу субагента и не выводить его находки заново.
- Несколько независимых субагентов запускать одним сообщением, чтобы шли параллельно.

## Автоматические триггеры скиллов

**Правило**: Claude определяет ситуацию → предлагает скилл → ждёт подтверждения.
Формат: `"Ситуация X → Запускаем /скилл или пропускаем?"`

### Приоритет вызова (от высшего к низшему)
1. *(первое сообщение сессии)* → `/brainstorming`; **но если это системная/межсервисная архитектура (Fable-fit)** → сначала предложить выбор «`/brainstorming` или `@architect` (Fable)», дождаться ответа
2. **Любой написанный/изменённый код перед "готово"** → `/review` (автоматически предлагать, не ждать пока спросят)
3. Баг, ошибка, exception, failed, "не работает", "сломалось" → `/systematic-debugging`
4. "посмотри модуль", "что там с...", "разберись" → `/research`
5. "изучи технологию", "сравни X vs Y", "что лучше для", "обзор рынка" → `/deep-research`
6. "сделай", "добавь фичу", "модуль", "реализуй" → `/build`
7. Перед написанием любого production-кода → `/test-driven-development`
8. 3+ независимых подзадачи в плане → `/subagent-driven-development`
9. 3+ независимых **крупных** домена одновременно (каждый заметно больше, чем несколько tool-call) → `/dispatching-parallel-agents`
10. Получен фидбек от ревьюера → `/receiving-code-review`
11. Заявляешь «готово / работает / тесты прошли», но вывода команды в этом сообщении нет → `/verification-before-completion`
12. Тесты прошли, нужен merge/PR → `/finishing-a-development-branch`
13. Нужна изоляция для feature → `/using-git-worktrees`
14. "перед деплоем", "выкати", "запусти" → `/review` (явный триггер от пользователя; автоматический при сборке кода — см. п.2)
15. "напиши тесты", "покрытие" → `/test`
16. "задокументируй", "документация" → `/docs`
17. "отчёт", "итоги", "результат" → `/report`
18. "напиши ТЗ", "составь тз", "сделай спеку", "техническое задание" → `/tz`
19. "аудит сервера", "проверь прод", "ротация учёток", "что крутится на сервере" → `/audit-server`
20. "реализуй по ТЗ", "сделай по спеке", "запусти имплементацию", "TDD по задаче" → `/spec-to-code`
21. "проверь API", "проверь контракты", "схемы API", "стыки интеграций" → `/api-contract-guardian`
22. "настрой CI/CD", "деплой пайплайн", "github actions", "docker setup" → `/cicd-quick-setup`
23. "проверь зависимости", "уязвимости пакетов", "npm audit", "обнови пакеты" → `/dependency-optimizer`
24. "обработка ошибок", "стандартизируй ошибки", "логирование ошибок" → `/error-handling-standardizer`
25. "медленно", "тормозит", "оптимизация", "узкие места", "performance" → `/performance-scanner`
26. "наведи порядок в скиллах", "ревизия скиллов", "дубли скиллов", "проверь скиллы" → `/skill-stocktake`
27. "сколько жрут скиллы", "контекст распух", "токен-бюджет", "что грузится в контекст" → `/context-budget`
28. "подними принципы в правила", "вынеси в правила", "дистилляция правил" → `/rules-distill`
29. **ВСЕГДА, на ЛЮБОМ сообщении сессии** (не только первом): системная/межсервисная архитектура, схемы данных, стратегия миграции, разбор системы целиком → предложить субагента `@architect` (Fable). *(это субагент, не скилл — формат «Ситуация → зову @architect (Fable) или пропускаем?»)* Модульная архитектура — по умолчанию Opus; на `@architect` эскалировать только если дизайн модуля реально сложный.

## Определение стека
- Стек проекта определяй из проектного `CLAUDE.md` или `package.json` / `go.mod` / `composer.json` / `pubspec.yaml`
- Тестовый фреймворк, линтер, структуру — адаптируй под стек
