---
name: context-budget
model: sonnet
description: Аудит потребления контекстного окна — CLAUDE.md, правила, MEMORY.md, описания скиллов, MCP-серверы. Находит распухание и дубли, выдаёт приоритизированный список где сэкономить токены. Только отчёт, ничего не правит.
allowed-tools: Read, Grep, Glob
argument-hint: "[пусто | '--verbose' для разбивки по файлам]"
---

Посчитай, сколько контекста съедают мои настройки: $ARGUMENTS

Claude в начале каждой сессии подгружает CLAUDE.md, правила, описания скиллов, схемы MCP-тулов — всё это занимает часть окна ещё до первого сообщения. Этот скилл считает, кто сколько ест, и где можно ужать. **Ничего не редактирует — только отчёт.**

## Что сканировать

Сначала определи корень текущего проекта: ближайший родительский каталог с `.git` или проектным `CLAUDE.md`. Ниже он обозначен как `<project_root>`.

**Цепочка CLAUDE.md**
- `$HOME/.claude/CLAUDE.md` (глобальный)
- `<project_root>/CLAUDE.md` и `<project_root>/.claude/CLAUDE.md` (проектные, если существуют)
- `<project_root>/.claude/rules/*.md` (грузятся как правила проекта)
- Считай токены каждого, флаг: суммарно >300 строк.

**MEMORY.md**
- `$HOME/.claude/projects/<идентификатор-текущего-проекта>/memory/MEMORY.md` (найди каталог, соответствующий `<project_root>`)
- Грузится каждую сессию. Флаг: >100 строк.

**Скиллы** (`SKILL.md` в `$HOME/.claude/skills/` и `<project_root>/.claude/skills/`)
- В контекст при старте грузится **только frontmatter (`description`)** каждого скилла, не тело. Считай длину description.
- Флаг: description >30 слов (раздут — грузится всегда), тело >400 строк (тяжёлый при вызове).

**MCP-серверы** (`<project_root>/.mcp.json`, `<project_root>/.claude/mcp.json`, если существуют, + активные)
- Посчитай серверы и общее число тулов. Схема тула ≈ 500 токенов.
- В этом сетапе: Trello (много тулов), Figma (очень много), Tempo. Флаг: сервер с >20 тулов, сервер-обёртка над CLI (gh, git, supabase).

## Оценка токенов

- Проза: `слова × 1.3`
- Код/таблицы: `символы / 4`
- MCP-тул: ~500 токенов схемы

## Классификация

| Корзина | Критерий | Действие |
|---------|----------|----------|
| **Нужно всегда** | Упомянуто в CLAUDE.md, стоит за активным триггером, под текущий стек | Оставить |
| **Иногда** | Доменно-специфичное, не в CLAUDE.md | По требованию / ленивая загрузка |
| **Редко** | Нет триггера, пересекается с другим, не подходит проекту | Убрать / выгрузить |

## Проблемные паттерны

- **Раздутые description скиллов** — >30 слов, грузятся в контекст всегда.
- **Дубли** — скилл дублирует другой скилл; правило дублирует CLAUDE.md.
- **MCP-перегруз** — самый большой рычаг: 30-тульный сервер весит больше всех скиллов вместе. Особенно Figma, если её тулы реально не нужны в большинстве сессий.
- **Распухание CLAUDE.md** — многословные объяснения, протухшие секции, инструкции, которым место в правилах.

## Отчёт

```
Бюджет контекста
═══════════════════════════════════════
Суммарный оверхед: ~XX,XXX токенов
Окно: ~XXX,XXX токенов → занято XX%

┌─────────────────┬───────┬───────────┐
│ Компонент       │ Кол-во│ Токены    │
├─────────────────┼───────┼───────────┤
│ CLAUDE.md       │ N     │ ~X,XXX    │
│ Правила         │ N     │ ~X,XXX    │
│ MEMORY.md       │ 1     │ ~X,XXX    │
│ Скиллы (descr.) │ N     │ ~X,XXX    │
│ MCP-тулы        │ N     │ ~XX,XXX   │
└─────────────────┴───────┴───────────┘

⚠️ Найдено проблем (N) — по убыванию экономии:

Топ-3 оптимизации:
1. [действие] → ~X,XXX токенов
2. [действие] → ~X,XXX токенов
3. [действие] → ~X,XXX токенов

Потенциальная экономия: ~XX,XXX токенов (XX% оверхеда)
```

В режиме `--verbose` добавь: токены по каждому файлу, построчную разбивку самых тяжёлых, конкретные дублирующиеся строки между пересекающимися компонентами, список MCP-тулов с оценкой размера каждого.

## Best practices

- **MCP — главный рычаг.** Каждая схема тула ~500 токенов; один многотульный сервер дороже всех скиллов. Если Figma нужна в 1 сессии из 20 — кандидат на отключение по умолчанию.
- **Description'ы грузятся всегда**, даже если скилл ни разу не вызван. Тело — нет.
- **Запускай после изменений** — добавил скилл/правило/MCP → прогони, чтобы поймать распухание рано.
- `--verbose` — для отладки «что именно весит», не для регулярного аудита.
