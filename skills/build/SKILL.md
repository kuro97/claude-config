---
name: build
description: Оркестратор разработки — сам определяет тип задачи и последовательно проходит все нужные этапы с подтверждением на каждом шаге
allowed-tools: Read, Grep, Glob, Agent, Write, WebSearch, WebFetch, Skill
argument-hint: "[описание задачи: новый модуль / доработка / баг]"
---

## Задача: $ARGUMENTS

---

## Принципы оркестрации

**Каждый этап = отдельный subagent через Agent tool.**

Зачем: свежий контекст на каждом этапе предотвращает "context rot".

**Модель на каждый этап** (передавай параметром `model` в вызов Agent):
| Этап | Модель |
|------|--------|
| Архитектор | запускать как `subagent_type: architect` (Fable) — БЕЗ параметра `model`, модель берётся из агента |
| Backend, Frontend, Security, Review, DevOps (прод), Диагност | `opus` |
| БА, Docs, QA (тесты + прогон), DevOps (конфиги), Финальный отчёт | `sonnet` |

Если Fable недоступна/лимит/refusal — этап Архитектора запускай с `model: opus` и сообщи об этом.
Этап 9 (Review) диспатчь сразу с `model: opus` — НЕ вызывай вложенно `/review` через Skill (двойной пин модели внутри субагента).
Оркестратор НЕ выполняет этапы сам — он формирует prompt, запускает Agent, получает structured summary, показывает пользователю, ждёт подтверждения.

**Перед первым этапом предложи `/effort xhigh`.** Субагенты наследуют сессионный effort, а глобальный дефолт — `high`. Для кодящих этапов (Backend/Frontend) и Архитектора это заметная разница. Формат: «Длинный агентный прогон → поднимаем `/effort xhigh` или идём на `high`?».

Этапы 7–9 (QA-прогон, Security, Review) — **проверяющие субагенты, они часть конструкции скилла**. Глобальный кэп «не делегировать верификацию» на них не распространяется, пропускать их нельзя.

**Цикл каждого этапа:**
1. Оркестратор формирует prompt для subagent (задача + summary предыдущих этапов)
2. Оркестратор запускает Agent с этим prompt
3. Subagent выполняет работу, возвращает structured summary
4. Оркестратор извлекает summary, показывает пользователю
5. Пользователь подтверждает / просит правки
6. Если правки — оркестратор запускает новый Agent с уточнениями
7. Summary сохраняется, переход к следующему этапу

**Оркестратор хранит в своём контексте ТОЛЬКО:**
- Исходную задачу ($ARGUMENTS)
- Тип задачи и список этапов
- Accumulated summaries (structured)
- Статус каждого этапа

**Оркестратор НЕ хранит:**
- Полный вывод subagent-ов (только summary)
- Содержимое файлов
- Детали реализации

---

## Формат structured summary

Каждый subagent ОБЯЗАН вернуть результат в этом формате:

```
STAGE_SUMMARY:
  stage: [номер и название]
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [ключевое решение 1]
    - [ключевое решение 2]
  artifacts:
    - [файл создан/изменён: путь — назначение]
  key_facts:
    - [факт который нужен следующим этапам]
  concerns:
    - [проблема или риск, если есть]
  next_stage_needs:
    - [что следующий этап должен знать]
```

**Правила передачи контекста subagent-у:**

Subagent получает prompt из 4 блоков:
1. **TASK** — исходное описание задачи (неизменно для всех этапов)
2. **SPEC** — summaries этапов 1-3 (БА, Архитектор, Docs) — передаются ПОЛНОСТЬЮ всем последующим этапам, это "спецификация проекта"
3. **PROGRESS** — summaries этапов 4+ — передаются сжато (только decisions + artifacts + concerns)
4. **STAGE_INSTRUCTIONS** — конкретные инструкции для текущего этапа

**Правило сжатия PROGRESS:**
- Из каждого summary этапа 4+ берём только: decisions, artifacts, concerns
- key_facts и next_stage_needs включаются в STAGE_INSTRUCTIONS если релевантны
- Это предотвращает линейный рост контекста

**Исключение:** Этап 12 (Финальный отчёт) получает ВСЕ summaries полностью.

---

## Шаг 0: Определение типа задачи

Проанализируй задачу и определи тип:

**Новый модуль** — нужен полный цикл от требований до деплоя
**Доработка** — изменение существующего функционала
**Багфикс** — поиск и исправление ошибки

Покажи пользователю:
```
Тип задачи: [тип]
Стек проекта: [определи из CLAUDE.md / package.json / go.mod / composer.json / pubspec.yaml]
Этапы которые нужны: [список]
Пропускаем: [список ненужных этапов и почему]
```
**Жди подтверждения перед стартом**

После подтверждения инициализируй:
```
BUILD_CONTEXT:
  task: $ARGUMENTS
  type: [определённый тип]
  stack: [стек проекта]
  stages: [список этапов]
  summaries: {}
```

---

## Этапы по типу задачи

### Новый модуль — все этапы
1. БА → 2. Архитектор → 3. Docs → 4. QA (тесты TDD) → 5. Backend → 6. Frontend → 7. QA (прогон) → 8. Security → 9. Review → 10. DevOps → 11. Docs (обновление) → 12. Финальный отчёт

### Доработка — без полного БА
1. Research → 2. Архитектор → 3. QA (тесты TDD) → 4. Backend → 5. Frontend (если нужно) → 6. QA (прогон) → 7. Review → 8. DevOps → 9. Docs (обновление)

### Багфикс — быстрый цикл
1. Диагност → 2. QA (тест на баг) → 3. Backend/Frontend → 4. QA (прогон) → 5. Review → 6. Docs (если затронута логика)

---

## Этап 1: Бизнес-аналитик

**Роль subagent**: Понять ЧТО нужно сделать и ЗАЧЕМ

**Оркестратор формирует prompt:**

```
Ты — Бизнес-аналитик. Твоя задача — сформулировать требования.

TASK: {$ARGUMENTS}
STACK: {стек проекта}

Выполни:
1. Сформулируй требования к задаче
2. Определи пользовательские сценарии (happy path + edge cases)
3. Найди зависимости от существующих модулей (используй Grep, Glob, Read)
4. Поищи аналогичные решения (WebSearch) если задача нетривиальная

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "1. Бизнес-аналитик"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [каждое требование как решение]
  artifacts: []
  key_facts:
    - Требования: [список]
    - Сценарии: основной + edge cases
    - Зависимости: модули, таблицы, API
  concerns:
    - [блокеры или неясности, если есть]
  next_stage_needs:
    - [что Архитектор должен учесть]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY из ответа subagent
2. Сохрани в summaries["stage_1"]
3. Покажи пользователю summary в читаемом виде
4. **"Требования готовы. Переходим к архитектуре?"**

---

## Этап 2: Архитектор

**Роль subagent**: Спроектировать КАК это сделать

**Оркестратор формирует prompt:**

```
Ты — Архитектор. Спроектируй решение.

TASK: {$ARGUMENTS}
STACK: {стек проекта}

SPEC:
{summaries["stage_1"] — полностью}

Выполни:
1. Определи какие файлы создать / изменить (чек-лист)
2. Спроектируй структуру БД: новые таблицы, изменения существующих
3. Спроектируй API эндпоинты (если нужно)
4. Учти архитектуру и стек проекта
5. Проверь что не дублируем существующий код (используй Grep, Glob)

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "2. Архитектор"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [каждое архитектурное решение]
  artifacts:
    - [файл для создания: путь — назначение]
    - [файл для изменения: путь — что меняем]
  key_facts:
    - Файлы для создания: [список]
    - Файлы для изменения: [список]
    - Изменения БД: [список или "нет"]
    - API эндпоинты: [список или "нет"]
  concerns:
    - [риски, сложности]
  next_stage_needs:
    - [что нужно задокументировать]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_2"]
2. Покажи пользователю чек-лист файлов и решений
3. **"Архитектура готова. Документируем перед кодом?"**

---

## Этап 3: Документация (ДО кода)

**Роль subagent**: Задокументировать что будем строить

**Оркестратор формирует prompt:**

```
Ты — Технический писатель. Создай документацию ДО написания кода.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_1"] — полностью}
{summaries["stage_2"] — полностью}

На основе требований БА и решений Архитектора создай:
- README модуля — назначение, как работает, зависимости
- Схема БД — таблицы, поля, связи (если новые/изменённые)
- API эндпоинты — если есть
- Бизнес-логика — основные процессы, потоки данных

Сохрани в _docs/[модуль]/

Зачем до кода: документация описывает НАМЕРЕНИЕ. После кода — описывает реализацию. Расхождение — сигнал проблемы.

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "3. Документация (до кода)"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [что задокументировано]
  artifacts:
    - [каждый созданный файл документации: путь — содержание]
  key_facts:
    - Документы созданы: [список]
  concerns: []
  next_stage_needs:
    - [на что опираться при написании тестов]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_3"]
2. Покажи список созданных документов
3. **"Документация создана. Переходим к тестам (TDD)?"**

---

## Этап 4: QA — Тесты ДО кода (TDD)

**Роль subagent**: Написать тесты ПЕРЕД кодом

**Оркестратор формирует prompt:**

```
Ты — QA-инженер. Напиши тесты ДО написания кода (TDD подход).

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_1"] — полностью}
{summaries["stage_2"] — полностью}
{summaries["stage_3"] — полностью}

Подход TDD:
1. На основе требований и архитектуры — напиши тесты
2. Тесты описывают ЧТО должно работать, ещё без реализации
3. Все тесты будут падать (красные) — это нормально
4. Backend и Frontend потом напишут код чтобы тесты стали зелёными

Определи тестовый фреймворк по стеку проекта (Jest, PHPUnit, Go test, Flutter test и т.д.)

Напиши тесты на:
- Основные сценарии из требований БА
- Edge cases (пустые данные, ошибки, граничные значения)
- Интеграции (БД, API)

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "4. QA TDD"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - Фреймворк: [выбранный]
    - Подход: [что тестируем]
  artifacts:
    - [каждый файл тестов: путь — что покрывает]
  key_facts:
    - Тестов написано: X (все красные)
    - Покрытие сценариев: [список]
  concerns:
    - [что сложно протестировать]
  next_stage_needs:
    - [какие тесты должен пройти Backend]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_4"]
2. Покажи количество тестов и покрытие
3. **"Тесты написаны (красные). Переходим к backend-коду?"**

---

## Этап 5: Backend-разработчик

**Роль subagent**: Написать серверный код чтобы тесты стали зелёными

**Оркестратор формирует prompt:**

```
Ты — Backend-разработчик. Напиши серверный код.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_1"] — полностью}
{summaries["stage_2"] — полностью}
{summaries["stage_3"] — полностью}

PROGRESS:
{summaries["stage_4"] — decisions + artifacts + concerns}

Выполни:
1. Следуй архитектуре из SPEC (stage 2)
2. Цель: пройти тесты из stage 4
3. Используй безопасные практики (prepared statements, экранирование)
4. Не хардкодь credentials
5. Логируй ошибки
6. Покажи код пользователю перед записью в файл

После написания — прогони тесты.

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "5. Backend"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [архитектурные решения при реализации]
  artifacts:
    - [каждый созданный/изменённый файл: путь — что сделано]
  key_facts:
    - Тесты: X/Y зелёных
    - [ключевые моменты реализации]
  concerns:
    - [отклонения от архитектуры, проблемы]
  next_stage_needs:
    - [что Frontend должен учесть]
    - [что QA должен проверить]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_5"]
2. Покажи какие файлы созданы, статус тестов
3. **"Backend готов. Переходим к frontend?"**

---

## Этап 6: Frontend-разработчик

**Роль subagent**: Сверстать интерфейс и клиентскую логику

**Оркестратор формирует prompt:**

```
Ты — Frontend-разработчик. Сверстай интерфейс.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_1"] — полностью}
{summaries["stage_2"] — полностью}
{summaries["stage_3"] — полностью}

PROGRESS:
{summaries["stage_4"] — decisions + artifacts + concerns}
{summaries["stage_5"] — decisions + artifacts + concerns}

Выполни:
1. Шаблоны / компоненты (по стеку проекта)
2. Стили (учти существующий стиль проекта)
3. Валидация форм на клиенте + на сервере
4. Экранирование вывода (XSS)
5. UX: сообщения об ошибках, loading states

Покажи шаблоны/вёрстку пользователю перед записью.

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "6. Frontend"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [решения по UI/UX]
  artifacts:
    - [каждый созданный/изменённый файл: путь — что сделано]
  key_facts:
    - Компоненты: [список]
    - Зависимости добавлены: [список или "нет"]
  concerns:
    - [проблемы совместимости, UX]
  next_stage_needs:
    - [что QA должен проверить в UI]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_6"]
2. Покажи список компонентов
3. **"Frontend готов. Прогоняем все тесты?"**

---

## Этап 7: QA — Прогон тестов

**Роль subagent**: Финальная проверка — все тесты должны быть зелёными

**Оркестратор формирует prompt:**

```
Ты — QA-инженер. Прогони все тесты и проверь интеграцию.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_1"] — полностью}
{summaries["stage_2"] — полностью}

PROGRESS:
{summaries["stage_4"] — decisions + artifacts}
{summaries["stage_5"] — decisions + artifacts}
{summaries["stage_6"] — decisions + artifacts}

Выполни:
1. Прогони все тесты из stage 4
2. Проверь интеграцию Backend + Frontend
3. Проверь что существующий функционал не сломан (регрессия)

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "7. QA прогон"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - Вердикт: все тесты зелёные / есть проблемы
  artifacts: []
  key_facts:
    - Тесты: X/X зелёных
    - Регрессия: не затронуто / проблемы
  concerns:
    - [упавшие тесты, проблемы интеграции]
  next_stage_needs:
    - [что Security должен проверить]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_7"]
2. Покажи результаты тестов
3. Если есть упавшие — вернись к этапу 5 или 6
4. **"Все тесты зелёные. Проверяем безопасность?"**

---

## Этап 8: Security-ревью

**Роль subagent**: Проверить безопасность нового кода

**Оркестратор формирует prompt:**

```
Ты — Security-инженер. Проведи аудит безопасности.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_2"] — полностью (архитектура)}

PROGRESS:
Файлы для проверки:
{собрать все artifacts из summaries stage_5 + stage_6}

Concerns из предыдущих этапов:
{собрать concerns из всех summaries}

Проверь:
1. Инъекции (SQL, NoSQL, command injection)
2. XSS в выводе
3. Авторизация и права доступа
4. Валидация входных данных
5. Утечка чувствительных данных в логах/ответах

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "8. Security"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - Вердикт: безопасно / найдены уязвимости
  artifacts: []
  key_facts:
    - Критичные: [список или "нет"]
    - Предупреждения: [список или "нет"]
  concerns:
    - [каждая уязвимость с severity]
  next_stage_needs:
    - [что нужно исправить перед review]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_8"]
2. Покажи результат аудита
3. Если критичные уязвимости — запусти fix-subagent, затем повтори security
4. **"Безопасность проверена. Запускаем code review?"**

---

## Этап 9: Code Review

**Роль subagent**: Финальная проверка всего кода перед деплоем

**Оркестратор формирует prompt:**

```
Ты — Code Reviewer. Проведи полное ревью кода.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_1"] — полностью}
{summaries["stage_2"] — полностью}

PROGRESS:
Файлы созданные/изменённые:
{собрать все artifacts из summaries stage_4 через stage_8}

Concerns из предыдущих этапов:
{собрать все concerns из summaries}

Выполни:
1. Отревьюй все созданные/изменённые файлы инлайн — НЕ вызывай `/review` через Skill tool (ты уже на opus, вложенный пин модели ломает этап). Смотри: корректность логики, обработка ошибок и edge cases, валидация ввода, побочные эффекты, утечки ресурсов, соответствие конвенциям проекта
2. Проверь соответствие реализации спецификации (stage 1 + stage 2)
3. Проверь что concerns из предыдущих этапов решены

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "9. Code Review"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - Вердикт: можно / с оговорками / нельзя
  artifacts: []
  key_facts:
    - Блокеры: [список или "нет"]
    - Предупреждения: [список или "нет"]
  concerns:
    - [нерешённые проблемы]
  next_stage_needs:
    - [что DevOps должен учесть при деплое]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_9"]
2. Покажи вердикт
3. Если "нельзя" — покажи блокеры, запусти fix-subagent, повтори review
4. **"Review завершён. Переходим к деплой-чеклисту?"**

---

## Этап 10: DevOps

**Роль subagent**: Подготовить к запуску

**Оркестратор формирует prompt:**

```
Ты — DevOps-инженер. Подготовь чек-лист деплоя.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_2"] — полностью (архитектура)}

PROGRESS:
Все артефакты:
{собрать artifacts из всех summaries}

Все concerns:
{собрать concerns из всех summaries}

Составь чек-лист деплоя:
- [ ] Бэкап БД
- [ ] Бэкап файлов
- [ ] Миграции БД (если есть)
- [ ] Конфиги (если менялись)
- [ ] Зависимости (npm install / composer install / go mod tidy и т.д.)
- [ ] Порядок выкатки файлов
- [ ] Откат: как вернуть назад
- [ ] Проверка после деплоя

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "10. DevOps"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - Порядок деплоя: [шаги]
    - Откат: [план]
  artifacts: []
  key_facts:
    - Чек-лист: [пункты]
    - Зависимости: [что установить]
    - Миграции: [есть/нет]
  concerns:
    - [риски при деплое]
  next_stage_needs:
    - [что обновить в документации]
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_10"]
2. Покажи чек-лист деплоя
3. **"Всё готово к деплою. Обновляем документацию?"**

---

## Этап 11: Документация (обновление)

**Роль subagent**: Обновить документацию по факту реализации

**Оркестратор формирует prompt:**

```
Ты — Технический писатель. Обнови документацию по факту реализации.

TASK: {$ARGUMENTS}

SPEC:
{summaries["stage_3"] — полностью (исходная документация)}

PROGRESS:
Все артефакты:
{собрать artifacts из всех summaries}

Все decisions:
{собрать decisions из всех summaries}

Выполни:
1. Сравни документацию из stage 3 с реальным кодом
2. Обнови если реализация отличается от плана
3. Добавь dev-guide.md если нужно
4. Добавь changelog запись

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "11. Документация (обновление)"
  status: DONE | DONE_WITH_CONCERNS | BLOCKED
  decisions:
    - [что обновлено / добавлено]
  artifacts:
    - [обновлённые файлы документации: путь — что изменено]
  key_facts:
    - Расхождения с планом: [список или "нет"]
  concerns: []
  next_stage_needs: []
```

**После получения результата:**
1. Извлеки STAGE_SUMMARY, сохрани в summaries["stage_11"]
2. Покажи что обновлено
3. **"Документация обновлена. Собираем финальный отчёт?"**

---

## Этап 12: Финальный отчёт

**Роль subagent**: Собрать итоговый отчёт по всем этапам

**ИСКЛЮЧЕНИЕ: этот subagent получает ВСЕ summaries полностью (без сжатия).**

**Оркестратор формирует prompt:**

```
Ты — Менеджер проекта. Собери финальный отчёт.

TASK: {$ARGUMENTS}

ВСЕ SUMMARIES (полностью):
{все summaries от stage_1 до stage_11 — без сжатия}

Собери итог по всем этапам:

Задача: [описание]
Тип: [новый модуль / доработка / багфикс]
Файлы создано: X
Файлы изменено: X
Тесты: X/Y зелёных
Безопасность: ок / проблемы
Code Review: вердикт
Готовность к деплою: ок / с оговорками / нет
Нерешённые concerns: [список или "нет"]

Сохрани отчёт в _audit/build_[название]_YYYY-MM-DD.md

ОБЯЗАТЕЛЬНО верни результат в формате:
STAGE_SUMMARY:
  stage: "12. Финальный отчёт"
  status: DONE
  decisions:
    - Готовность: [итоговый вердикт]
  artifacts:
    - [путь к файлу отчёта]
  key_facts:
    - [ключевые метрики]
  concerns:
    - [нерешённые проблемы]
  next_stage_needs: []
```

**После получения результата:**
1. Покажи пользователю итоговый отчёт
2. Работа завершена

---

## Правила оркестратора

1. **На каждом этапе спрашивай** — "Переходим к следующему?" / "Нужны правки?"
2. **Если пользователь говорит "пропусти"** — пропускай этап, сохрани в summaries как status: SKIPPED
3. **Если subagent вернул BLOCKED** — останови и сообщи сразу, не пытайся обойти
4. **Не передавай файлы в subagent** — передавай только summary и инструкции, subagent сам читает файлы
5. **Контекст передаётся ТОЛЬКО через summary** — НИКОГДА не копируй полный вывод subagent в prompt следующего
6. **Оркестратор НЕ кодит сам** — он формирует prompts, запускает Agent, парсит summary
7. **Если subagent вернул DONE_WITH_CONCERNS** — покажи concerns пользователю перед переходом
8. **Максимум 2 retry** — если subagent BLOCKED дважды на одном этапе, эскалируй к пользователю
9. **Этап 12 получает ВСЕ summaries полностью** — единственный этап без сжатия
10. **Не редактируй файлы** без показа чек-листа и одобрения пользователя

---

## Обработка статусов subagent

**DONE:** Извлеки summary, покажи пользователю, жди подтверждения.

**DONE_WITH_CONCERNS:** Покажи concerns отдельно. Спроси: "Решаем сейчас или двигаемся дальше?" Если решаем — запусти fix-subagent с конкретными concerns.

**BLOCKED:** Покажи причину блокировки. Варианты:
1. Не хватает контекста → дополни prompt, перезапусти
2. Задача слишком сложная → разбей этап на подэтапы
3. Конфликт с предыдущим этапом → вернись к предыдущему
4. Невозможно выполнить → эскалируй к пользователю

**SKIPPED:** Пользователь попросил пропустить. Сохрани summary с status: SKIPPED, decisions: ["Пропущен по решению пользователя"].

**Нет STAGE_SUMMARY в ответе:** Subagent забыл формат. Извлеки ключевую информацию из ответа, сформируй summary вручную, пометь concerns: ["summary извлечён оркестратором — возможна потеря деталей"].

---

## Пример работы оркестратора

```
Пользователь: /build Добавить систему уведомлений через Telegram

[Шаг 0: определяет тип]
Тип: Новый модуль → все 12 этапов
"Подтверждаете?"

[Этап 1: dispatch Agent — БА]
→ Agent получает prompt с TASK
→ Возвращает STAGE_SUMMARY (status: DONE, decisions: [...])
→ Оркестратор сохраняет summaries["stage_1"]
→ Показывает: "Требования: 5 пунктов, 3 сценария, 2 зависимости"
→ "Переходим к архитектуре?"

[Этап 2: dispatch Agent — Архитектор]
→ Agent получает TASK + SPEC (stage_1 полностью)
→ Возвращает STAGE_SUMMARY (artifacts: ["src/notifications/...", ...])
→ Оркестратор сохраняет summaries["stage_2"]
→ Показывает чек-лист файлов
→ "Архитектура готова. Документируем?"

...

[Этап 5: dispatch Agent — Backend]
→ Agent получает TASK + SPEC (stages 1-3 полностью) + PROGRESS (stage_4 сжато)
→ Пишет код, прогоняет тесты
→ Возвращает STAGE_SUMMARY (artifacts: [...], key_facts: ["Тесты: 8/10 зелёных"])
→ Оркестратор сохраняет
→ "Backend готов, 8/10 тестов. Переходим к frontend?"

[Этап 12: dispatch Agent — Финальный отчёт]
→ Agent получает ВСЕ summaries полностью (без сжатия)
→ Формирует отчёт, сохраняет в _audit/
→ "Готово!"
```
