---
name: plan
description: Собрать всю информацию и составить план, затем проверить его через Fable. research (Sonnet 5) → черновик плана (Opus 5) → верификация architect (Fable 5).
allowed-tools: Read, Grep, Glob, Agent, WebSearch, WebFetch
argument-hint: "[задача для планирования]"
---

Составь план по задаче и проверь его через Fable: $ARGUMENTS

Три шага, не пропускай:

## Шаг 1 — Сбор информации (Sonnet 5)
Запусти субагента `researcher` (subagent_type: researcher) с задачей: собрать всё релевантное по «$ARGUMENTS» — какие файлы задействованы, как оно сейчас работает, зависимости, ограничения. Получи карту фактов.

## Шаг 2 — Черновик плана (Opus 5, главный цикл)
На основе фактов из шага 1 составь план сам (ты на Opus 5): цель, шаги, файлы к изменению, риски, порядок выполнения, план отката. Покажи черновик пользователю.

## Шаг 3 — Верификация Fable
Отдай черновик на проверку: запусти `subagent_type: architect` (Fable) с полным текстом плана и заданием: «найди дыры, неверные допущения, пропущенные шаги и риски; верни вердикт GO / GO_WITH_CHANGES / REWORK + правки по приоритету».
- Если Fable недоступна или упёрлась в лимит — повтори тот же вызов с `model: opus` и сообщи, что проверку сделал Opus, а не Fable.
- Content-refusal (cyber/bio) харнесс обрабатывает сам: перезапускает на фолбэк-модели (Fable cyber→Opus 4.8, bio→Opus 5) и показывает уведомление в транскрипте. Увидел его — просто скажи пользователю, кто в итоге проверял план.

Покажи пользователю вердикт и правки, интегрируй их в финальный план.

**Не начинай реализацию** — `/plan` только планирует и проверяет. Реализация — отдельно (`/build` или вручную по плану).
